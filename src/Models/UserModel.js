
export function UserModel(firstName, lastName, email, userName, password, rememberMe)
{
  this.firstName = firstName;
  this.lastName = lastName;
  this.email = email;
  this.userName = userName;
  this.password = password;
  this.rememberMe = rememberMe;
}